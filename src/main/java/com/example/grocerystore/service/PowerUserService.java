package com.example.grocerystore.service;

import com.example.grocerystore.domain.entities.User;
import com.example.grocerystore.domain.models.service.UserServiceModel;

import java.util.List;

import org.springframework.stereotype.Service;

@Service

public interface PowerUserService 
{
	
	/*
	 * public void saveUserWithDefaultRole(User user);
	 * 
	 * List<UserServiceModel> findAllByPowerUsers();
	 */
	
	/*
	 * { BCryptPasswordEncoder encoder=new BCryptPasswordEncoder(); String
	 * encodedPwd=encoder.encode(user.getPassword()); user.setPassword(encodedPwd);
	 * Set<Role> roleUser= roleRepo.findByAuthority("ROLE_MODERATOR");
	 * user.setAuthorities(roleUser); puserRepo.save(user);
	 * 
	 * }
	 */
	
	//public List<User> listAll
	
}
